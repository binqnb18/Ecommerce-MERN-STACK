import { Router } from 'express';

const router = Router();

// Đăng nhập admin
router.post('/login', (req, res) => {
  // Logic sẽ được thêm sau: xử lý đăng nhập admin
});

// Đăng xuất admin
router.post('/logout', (req, res) => {
  // Logic sẽ được thêm sau: xử lý đăng xuất admin
});

export default router;